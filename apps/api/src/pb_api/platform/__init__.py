"""Platform-level concerns that cut across product modules.

The module registry (``pb_api.platform.modules``) is the single source of truth
for which product modules exist, their reserved API namespaces, lifecycle
status, and any governance constraints that a future implementation must honour.
"""
